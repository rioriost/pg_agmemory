# Copy to .env locally and replace the placeholder; never commit the real key.
# The CLI reads process environment variables, not .env files automatically.
OPENAI_API_KEY=replace-with-your-openai-api-key
